"""Generic web-session helpers reused across the TaskVM apps (FIFO session cap
+ summary-only state payload). Contract shape adapted from SenseAct's
``web_helpers.py``; SenseAct-specific machinery is NOT ported (see
``web_helpers.py`` docstring)."""
