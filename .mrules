# TaskVM · Repository Contract

> 本文件是 TaskVM 仓库的最终规则文件，所有 coding agent 必读。
> 完整纠错历史见 `.mrules.log`（已冻结，不再追加）。
> 心智模型总纲见 `docs/A2UI_开工大纲_v0_心智模型对齐版.md`。

---

## 1. 五条结果性原则

1. **自底向上的实时投影**——展示的必须是"世界现在是什么样"，不是"Agent 打算做什么"。
2. **双向可执行性**——用户改任务变量 → Agent 写回真实应用 → 底层变化重新反映到投影。
3. **Substrate independence**——同一任务意图在不同软件栈上语义一致，底层执行轨迹可以不同。
4. **Governance over autonomy**——人治理目标/约束/checkpoint/方向，Agent 在边界内持续自治。
5. **独立验证与诚实可逆性**——verifier 读 fresh visible observation 判定完成；不可逆动作诚实标记。

---

## 2. 六层依赖方向（禁止逆流）

```
domain ← kernel ← architect ← governance ← runtime ← projection
                                     ↘ substrate port ↗
```

- `taskvm/` 零引用 `taskvm_bench/`（架构门锁定）。
- benchmark 层单向导入 prototype（SUT 方向）。
- 每层只通过公开 port 与相邻层交互，不跨层 import。

---

## 3. GUI-only · 无内部 ID 暴露

**判定口令**：*"这个字符串，真实用户能在渲染屏幕上肉眼看到吗？"*

- 看不到 → 禁止进入任何模型输入。
- 禁止：entity_id、data-\*-id、get_state 产物、operator 内部词汇、深链 URL。
- 写/回退路径必须经过真实 GUI 手势，任何脱离渲染页面的写操作都是后门。

---

## 4. Runtime / Evaluation 权限隔离

- `taskvm/` = prototype（枪 + 靶场）。
- `taskvm_bench/` = 计量仪器（benchmark / evaluation / baselines）。
- 评测环境的 reset / seed 权限不能成为 runtime 正常运行能力的一部分。
- `eval_results/` 目录不进 git。

---

## 5. Owned Paths

| 层 | Owner path | 冻结状态 |
|---|---|---|
| domain / kernel | `taskvm/domain/` `taskvm/kernel/` | FROZEN |
| architect | `taskvm/architect/` | FROZEN |
| substrate | `taskvm/substrate/` | FROZEN |
| governance | `taskvm/governance/` | FROZEN |
| runtime | `taskvm/runtime/` | FROZEN |
| projection | `taskvm/projection/` | FROZEN |
| workspace_ui | `taskvm/workspace_ui/` | ACTIVE |
| benchmark | `taskvm_bench/` | FROZEN |
| scripts / configs / docs / README / pyproject | 根目录文件 | ACTIVE |

修改冻结层需 RFC + exact-SHA 审计证据。不越层修改。

---

## 6. 测试与诚实汇报

- **验证性结论必须附 `eval_results/*.json` 原始字段**，不接受口头汇报替代。
- **发现失败必须诚实报告**，禁止用 mock/api 路径数字掩盖真实 GUI 失败。
- **Gate 判据用 mean/majority，不用 max**；1 次幸运 ≠ PASS。
- **被问到 A 时主动检查 B/C/D**（元原则 9）。
- 合同锁测试是系统的质检章，不是脚手架——枪造好后每次改装仍要过规。

---

## 7. Git 提交纪律

- 多 agent 并行工作时，commit 时只 `git add` 自己本会话修改的文件。
- 不用 `git add -A` 或 `git add .`。
- `eval_results/`、`*.png`、截图目录不进 staging。
- 一个 commit 一个性质（纯 move / 纯 fix / 纯 doc），不混入"顺手改进"。

---

## 8. 开发环境

- **标准 conda 环境**：`taskvm`（Python 3.10）。
- **运行命令**：通过 `TASKVM_PYTHON` 环境变量指定解释器，或使用 `scripts/dev.sh`。
- **Playwright 浏览器**：标准安装发现 + interpreter-derived `opt/ms-playwright` fallback。
- **启动**：`./scripts/dev.sh` / 停止：`./scripts/stop.sh`。

---

## 9. 不再追加

本文件是最终 repository contract。不再追加 episode、phase、gate、killtest 历史。
后续 issue 进入正常 issue tracker，不扩写本文件。
历史纠错记录保留在 `.mrules.log`（已冻结）。
